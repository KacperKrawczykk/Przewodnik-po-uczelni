@model IEnumerable<NowyPrzewodnikMVC.Models.Waypoint>

@{
    ViewData["Title"] = "Panel Admina";
}

<div class="container mt-5" style="max-width: 1200px;">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h5 class="text-white-50 text-uppercase mb-0 small">Panel Zarządzania</h5>
            <h2 class="text-white fw-bold">📍 Lista Lokacji</h2>
        </div>
        <div>
            <a asp-action="Create" class="btn btn-success btn-lg shadow fw-bold">
                ➕ Dodaj Nowe Miejsce
            </a>
            <a asp-action="Logout" class="btn btn-outline-danger btn-lg ms-2">
                Wyloguj
            </a>
        </div>
    </div>

    <div class="card bg-dark border-secondary shadow-lg">
        <div class="card-body p-0">
            <table class="table table-dark table-hover table-striped mb-0 align-middle">
                <thead class="bg-black text-uppercase small text-muted">
                    <tr>
                        <th class="py-3 ps-4">ID</th>
                        <th class="py-3">Podgląd</th>
                        <th class="py-3">Nazwa Miejsca</th>
                        <th class="py-3 text-end pe-4">Zarządzanie</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model) {
                        <tr>
                            <td class="ps-4 fw-bold text-muted">#@item.Id</td>
                            <td>
                                <img src="@item.ImageUrl" 
                                     style="width: 80px; height: 50px; object-fit: cover; border-radius: 6px; border: 1px solid #555;" 
                                     onerror="this.src='https://placehold.co/80?text=?'">
                            </td>
                            <td class="fw-bold fs-5">@item.Name</td>
                            <td class="text-end pe-4">
                                <div class="btn-group" role="group">
                                    <a asp-action="Connections" asp-route-id="@item.Id" class="btn btn-sm btn-info text-white" title="Edytuj Trasy">
                                        🔀 Trasy
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning" title="Edytuj Opis/Foto">
                                        ✏️ Edytuj
                                    </a>
                                    <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;" onsubmit="return confirm('Czy na pewno usunąć?');">
                                        <button type="submit" class="btn btn-sm btn-danger" title="Usuń">🗑️</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>